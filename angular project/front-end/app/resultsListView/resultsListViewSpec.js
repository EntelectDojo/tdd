'use strict';

describe('when the results list view loads', function() {

    beforeEach(module('resultsListView'));

	it('should ....', inject(function($controller) {
	  expect(true).toEqual(true);
	}));

});